import Foundation

// Copy this file to `Config.swift` and replace the placeholder value with your real API key.
// Keep `Config.swift` out of version control.
enum AppConfig {
    static let openAIAPIKey: String = "YOUR_API_KEY_HERE"
}
